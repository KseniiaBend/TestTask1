import { CARDS, SETS } from 'src/utils/constants';

export const BASE_URL = process.env.REACT_APP_BASE_URL;
export const CARDS_URL = `${BASE_URL}${CARDS}/`;
export const SETS_URL = `${BASE_URL}${SETS}/`;
